import { TypographyProps } from "@mui/material";
import Typography from "@mui/material/Typography";

function PayaTypography(props: TypographyProps) {
    return <Typography {...props} />;
}

export default PayaTypography;
