import { TypographyProps } from "@mui/material";
import Typography from "@mui/material/Typography";

function ReihanTypography(props: TypographyProps) {
    return <Typography {...props} />;
}

export default ReihanTypography;
